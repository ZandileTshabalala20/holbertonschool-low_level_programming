#include "main.h"

/**
 * print_most_numbers -  prints the numbers, from 0 to 9 expect for 2 and 4.
 *
 * Return: Always 0.
 */
void print_most_numbers(void)
{
	int x;

	for (x = 48; a <= 58; x++)
	{
		if (x != 50)
		{
			if (x != 52)
			{
			_putchar(x);
			
		}
	}
	_putchar('\n');
}

